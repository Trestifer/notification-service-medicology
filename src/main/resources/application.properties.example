server.port=${PORT:8085}
spring.application.name=notification-service-medicology

notification.datasource.url=${NOTIFICATION_DATABASE_URL}
notification.datasource.username=${NOTIFICATION_DB_USERNAME}
notification.datasource.password=${NOTIFICATION_DB_PASSWORD}
notification.datasource.driver-class-name=org.postgresql.Driver

learning.datasource.url=${LEARNING_DATABASE_URL}
learning.datasource.username=${LEARNING_DB_USERNAME}
learning.datasource.password=${LEARNING_DB_PASSWORD}
learning.datasource.driver-class-name=org.postgresql.Driver

auth.datasource.url=${AUTH_DATABASE_URL}
auth.datasource.username=${AUTH_DB_USERNAME}
auth.datasource.password=${AUTH_DB_PASSWORD}
auth.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=none
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

jwt.secret=${JWT_SECRET}
jwt.issuer=${JWT_ISSUER:medicology-auth}
jwt.audience=${JWT_AUDIENCE:medicology-api}

app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:8085}

sendgrid.from-email=${SENDGRID_FROM_EMAIL:}
sendgrid.api.key=${SENDGRID_API_KEY:}

notification.reminder-zone=${NOTIFICATION_REMINDER_ZONE:Asia/Ho_Chi_Minh}
notification.reminder-check.cron=${NOTIFICATION_REMINDER_CHECK_CRON:0 * * * * *}
